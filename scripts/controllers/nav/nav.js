define('nav', ['app', 'config', 'bootstrap', 'galileo.ctrl'], function (app, config) {
	var navCtrl = app.controller('navController', ['$scope', '$location', '$routeParams', '$rootScope', function ($scope, $location, $routeParams, $rootScope) {

	}]);

	return navCtrl;
})